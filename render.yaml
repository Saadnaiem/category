services:
  - type: web
    name: category-management-ai
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn category_management_ai.web.api:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13